<h1>Order Details</h1>


<table>
  <thead>
    <tr>
      <th>Quantity</th>    
      <th>Unit Price</th> 
      <th>Total</th>    
      <th>Product ID</th> 
      <th>Date</th> 
      </tr>
  </thead>

  <tbody>
    <% @order.order_items.each do |line_item| %>
      <tr>
        <td><%= line_item.quantity %></td>
        <td><%= line_item.unit_price %></td>
        <td><%= line_item.total %></td>
        <td><%= line_item.product_id %></td>
        <td><%= line_item.created_at %></td>
    <% end %>
  </tbody>    
</table> <br>
 Total of your Order is:<%= @order.subtotal %>
 <br>
<%= link_to 'Go Back To Home Page', homepage_index_path %>
